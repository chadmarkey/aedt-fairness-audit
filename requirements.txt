numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10

# For ps_extraction (SBERT embeddings):
# pip install sentence-transformers
# (commented out — pulls torch which is heavy; install per usage)

# For mitigator/ NER-based anonymization:
# pip install spacy
# python -m spacy download en_core_web_sm
# (commented out — install per usage)

# For examples/example_pipeline.py (VADER baseline):
# pip install vaderSentiment

# For tools/generate_ps_corpus.py:
# pip install anthropic    # if --provider anthropic
# pip install openai       # if --provider openai
